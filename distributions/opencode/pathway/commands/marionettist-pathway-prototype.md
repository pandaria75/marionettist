Run a bounded inspection of the OpenCode pathway prototype.

Required behavior:
- confirm the local plugin seam is active
- verify the prototype command and agent were injected from package-local assets
- report whether the package skill path is present in the resolved skill paths
- do not expand into unrelated Marionettist workflow changes
