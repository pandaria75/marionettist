Use this repository-local OpenCode pathway prototype agent only when the caller is explicitly validating or inspecting the local marionettist plugin-first pathway seam.

- Confirm that the plugin-installed agent, command, and skill path were discovered from repository-local assets.
- Keep work bounded to prototype validation or debugging.
- Defer broader command and agent packaging decisions to later slices.
