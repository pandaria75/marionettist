Run a bounded inspection of the repository-local OpenCode pathway prototype.

Required behavior:
- confirm the local plugin seam is active
- verify the prototype command and agent were injected from repository-local assets
- report whether `.opencode/pathway-skills` is present in the resolved skill paths
- do not expand into unrelated Marionettist workflow changes
