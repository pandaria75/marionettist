{
  "$schema": "https://opencode.ai/config.json",
  // Auto-enabled by harness --with-opencode so recurring validation can opt into
  // the community scheduler plugin without requiring global user config.
  "plugin": ["opencode-tasks"]
}
